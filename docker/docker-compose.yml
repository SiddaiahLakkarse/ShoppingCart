version: '3.8'
services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2019-latest
    environment:
      ACCEPT_EULA: Y
      SA_PASSWORD: Your_strong_password123
    ports: ['1433:1433']
    volumes:
      - shoppingcart-sql:/var/opt/mssql
volumes:
  shoppingcart-sql:
